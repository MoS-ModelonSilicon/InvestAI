[pytest]
testpaths = tests
addopts = -v --tb=short
markers =
    smoke: critical path tests (login, dashboard loads) — run on every PR
    core: core feature E2E tests — run on merge to main
    deep: deep regression tests — run nightly only

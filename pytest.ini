[pytest]
testpaths = tests
addopts = -v --tb=short
markers =
    smoke: fast API tests — run on every PR/push (~1-2 min, no browser)
    core: core feature E2E tests — run on merge to main
    deep: deep regression tests — run nightly only
